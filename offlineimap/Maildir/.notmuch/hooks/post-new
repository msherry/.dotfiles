#!/bin/bash

# TODO: these can fail if there are no results being tagged. `|| true` helps
# the hook as a whole succeed still, but this is kind of lame.

notmuch tag +thread_flagged $(notmuch search --output=threads tag:flagged) || true
notmuch tag +muted $(notmuch search --output=threads tag:muted) || true
notmuch tag -INBOX $(notmuch search --output=threads tag:muted and tag:INBOX) || true
